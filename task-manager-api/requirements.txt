# Runtime mínimo: Python 3.12 (a versão contra a qual TR-12 verificou as
# chamadas deprecated). Ver também .python-version.
#
# marshmallow e requests foram REMOVIDAS por TR-15/F-020: estavam declaradas e não
# eram importadas por arquivo algum. A validação declarativa que marshmallow
# prometia foi implementada em validators/ com a biblioteca padrão; requests não
# tinha correspondente no código.
flask==3.0.0
flask-sqlalchemy==3.1.1
flask-cors==4.0.0
python-dotenv==1.0.0
